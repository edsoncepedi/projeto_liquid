# Dependencias comuns aos tres servicos (desenvolvimento local).
# Cada imagem Docker instala apenas o que precisa (ver */requirements.txt).
pymodbus>=3.6,<3.8
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pyyaml>=6
pydantic>=2
pydantic-settings>=2
pytest>=8
pytest-asyncio>=0.23
